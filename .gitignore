# Dependencies
node_modules/
vendor/
.venv/
__pycache__/

# Build artifacts / compiled outputs
dist/
build/
out/
*.o
*.obj
*.class
*.pyc

# Environment secrets
.env
.env.*
!.env.example
*.pem
*.key

# Temporary / OS files
*.tmp
*.log
.DS_Store
Thumbs.db

# Django local dev database (settings.py uses SQLite for local dev/test
# only - production uses PostgreSQL per DBD-001, see IMP-001A)
src/db.sqlite3

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# NOTE: ai/ (the AI engineering framework) is intentionally NOT ignored.
# It must remain version-controlled per Constitution Rule 1.
